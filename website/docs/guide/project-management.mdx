---
sidebar_position: 4
description: 项目管理

options:
  menu:
    weight: 40
    category: guide
---

# 项目管理

import RunCLI from "@site/src/components/RunCLI";
import PluginHelpSource from "!!raw-loader!@site/src/scripts/nb_plugin.py";
import DriverHelpSource from "!!raw-loader!@site/src/scripts/nb_driver.py";
import AdapterHelpSource from "!!raw-loader!@site/src/scripts/nb_adapter.py";

:::warning 注意
请在运行命令前，确保已经在项目目录下。如果使用了虚拟环境，确保已经激活。
:::

## 插件管理

运行以下命令打开插件管理选单

```shell
nb plugin
```

<RunCLI
  code={PluginHelpSource}
  placeholder="正在加载 Python 模块..."
  className="language-shell"
/>

## 驱动器管理

运行以下命令打开驱动器管理选单

```shell
nb driver
```

<RunCLI
  code={DriverHelpSource}
  placeholder="正在加载 Python 模块..."
  className="language-shell"
/>

## 适配器管理

运行以下命令打开适配器管理选单

```shell
nb adapter
```

<RunCLI
  code={AdapterHelpSource}
  placeholder="正在加载 Python 模块..."
  className="language-shell"
/>
